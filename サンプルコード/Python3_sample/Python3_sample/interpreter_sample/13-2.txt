P311●テキストデータをファイルに書き込むfile = "sample.txt"fileobj = open(file, "w", encoding = "utf_8")    # 1.ファイルを開くfileobj.write("こんにちは\n")         # 2.テキストデータを書き込むfileobj.write("Pythonをはじめよう\n")  # 2.続きを書き込むfileobj.close()    # 3. ファイルを閉じるP312●with-as文を使ってテキストファイルを書き出すfile = "sample.txt"with open(file, "w", encoding = "utf_8") as fileobj:    fileobj.write("こんにちは\n")    fileobj.write("Pythonをはじめよう\n")P315●カレントディレクトリにdataフォルダがあるかどうかチェックするimport os    # osモジュールをインポートしておくos.path.exists("./data")   # dataフォルダがあるos.path.exists("./data2")  # data2フォルダはない